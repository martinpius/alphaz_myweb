# AlphaZ Data Solution

Static website prepared for GitHub Pages.

## Publish on GitHub Pages

1. Create a new public GitHub repository.
2. Upload all files from this folder to the repository root.
3. On GitHub, open `Settings`.
4. Open `Pages`.
5. Under `Build and deployment`, choose `Deploy from a branch`.
6. Select the `main` branch and the `/(root)` folder.
7. Save the settings and wait for deployment.

Your site will then be visible at:

- `https://<your-username>.github.io/<repo-name>/`

If you want the site at:

- `https://<your-username>.github.io/`

name the repository:

- `<your-username>.github.io`

## Included Files

- `index.html`
- `about.html`
- `consulting.html`
- `problem-solving.html`
- `contact.html`
- `styles.css`
- `script.js`
- `.nojekyll`

## Notes

- The site uses relative links, so it works on GitHub Pages as a project site.
- `.nojekyll` ensures GitHub Pages serves the files directly as a static site.
